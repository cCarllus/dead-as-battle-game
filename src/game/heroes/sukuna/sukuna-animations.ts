// Responsável por declarar o stub de mapeamento de animações do herói sukuna para expansão futura.
import type { HeroAnimationConfig } from "../../animation/animation-types";

export const SUKUNA_HERO_ANIMATION_CONFIG: HeroAnimationConfig = {
  heroId: "sukuna",
  commandToGroupName: {}
};
