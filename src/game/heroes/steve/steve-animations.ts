// Responsável por declarar o stub de mapeamento de animações do herói steve para expansão futura.
import type { HeroAnimationConfig } from "../../animation/animation-types";

export const STEVE_HERO_ANIMATION_CONFIG: HeroAnimationConfig = {
  heroId: "steve",
  commandToGroupName: {}
};
