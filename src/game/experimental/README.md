# Experimental Runtime

Use this folder for prototypes, spike implementations, and temporary gameplay experiments.

Rules:

- Experimental code must not become an implicit dependency of stable runtime modules.
- Promote code out of this folder only after it has a clear owner domain and tests/verification.
- If an experiment is abandoned, delete it or migrate the useful parts into the correct stable domain.
