# Legacy Frontend Archive

The previous browser client source and public assets were moved into this folder during the Babylon.js 8 code-first refactor on 2026-03-20.

Active frontend development now lives in the root `src/`, `public/`, and `assets/` directories.
